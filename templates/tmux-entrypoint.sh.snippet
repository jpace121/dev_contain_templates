echo "==> Set tmux as entry point."
buildah copy $container '{{ template_dir }}/entrypoints/tmux.bash' '/tmux.bash'
buildah config --entrypoint '["/tmux.bash"]' $container
buildah config --cmd '' $container